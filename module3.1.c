#include<stdio.h>
int main(){
    int year;
    printf("enter the year");
    printf("%d, &year");
    if(year % 100 == 0);
    {
      if(year % 400){
        printf("leap year");
    }
      else
    {
      printf("not a leap year");
    }
    }
}
